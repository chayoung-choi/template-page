# STAY LUMÉ 펜션 홈페이지 기본 구조 가이드

## 목적

이 프로젝트는 펜션·풀빌라 홈페이지를 빠르게 다시 만들 수 있도록 만든 단일 페이지 기본 템플릿입니다. `index.html`을 브라우저에서 열면 별도 설치 없이 확인할 수 있습니다.

## 파일 구성

| 파일 | 역할 |
| --- | --- |
| `index.html` | 메뉴, 섹션, 문구 등 페이지 구조 |
| `style.css` | 색상, 반응형 레이아웃, 애니메이션 |
| `script.js` | 모바일 메뉴 열기/닫기 |
| `image/` | 메인 및 객실 사진 자산 |

## 버전 관리 규칙

디자인 버전을 새로 요청할 때에는 기존 파일을 덮어쓰지 않습니다. 새 버전은 `index_v2.html`, `style_v2.css`, `script_v2.js`처럼 같은 버전 번호를 붙여 추가합니다. 현재 기본안은 `index.html`, 차분한 프리미엄 모션 디자인 버전은 `index_v2.html`, 2026년 예약 전환 중심 버전은 `index_v3.html`입니다. 모든 버전은 `versions.html`에서 한 번에 비교하고 열 수 있습니다.

## 기본 메뉴와 연결 위치

상단 메뉴는 각 섹션 ID로 이동합니다. 다른 펜션에 재사용할 때도 메뉴 이름과 해당 ID를 함께 수정하면 됩니다.

| 메뉴 | 섹션 ID | 권장 내용 |
| --- | --- | --- |
| ABOUT | `#about` | 펜션의 핵심 콘셉트와 차별점 |
| ROOMS | `#rooms` | 객실 목록, 인원, 상세 안내 |
| SPECIAL | `#experience` | 수영장, 바비큐, 조식 등 부대 경험 |
| GUIDE | `#guide` | 입·퇴실, 비품, 유의 사항 |
| LOCATION | `#location` | 주소, 연락처, SNS, 지도 링크 |
| RESERVATION | `#reserve` | 예약 채널 또는 예약 페이지 링크 |

## 다른 펜션으로 바꾸는 순서

1. `image/`에 새 사진을 넣고, `index.html`과 `style.css`의 이미지 경로를 교체합니다. 메인 사진은 `style.css`의 `.hero-image`, 하단 예약 배경은 `.reservation-bg`에서 바꿉니다.
2. `STAY LUMÉ`, 주소, 전화번호, 이메일, 객실명과 인원 정보를 실제 정보로 수정합니다.
3. 예약 링크를 실제 예약 서비스 URL로 연결합니다. 현재는 전화 연결 예시(`tel:010-0000-0000`)입니다.
4. 객실이 늘거나 줄면 `room-card` 블록 하나를 복사하거나 제거합니다. 객실 수가 3개가 아닐 경우 `.room-list`의 `grid-template-columns`도 함께 조정하면 균형이 좋습니다.
5. 브랜드 색상은 `style.css` 첫 줄의 `:root` 변수 `--ink`, `--cream`, `--lime` 값만 바꾸면 전체에 반영됩니다.

## 운영 전 확인할 항목

- 전화번호, 주소, 이메일, SNS 링크를 실제 정보로 교체
- 예약 버튼을 실제 예약 페이지·네이버 예약·카카오 채널 등에 연결
- 객실별 요금, 기준/최대 인원, 반려동물 및 취소 규정을 명시
- 지도·오시는 길, 주차, 주변 관광지 페이지를 필요에 따라 추가
- 사진 사용 권한 및 모바일에서 사진의 잘림 상태 확인

## 디자인 원칙

이 템플릿은 전형적인 펜션 사이트보다 문화 브랜드·에디토리얼 랜딩 페이지에 가까운 인상을 목표로 합니다. 큰 산세리프 타이포그래피와 강한 색 대비, 비대칭 사진 그리드로 첫 화면에서 브랜드를 인식시키고, 예약은 처음과 마지막에 분명히 제시합니다.

- 제목: `Syne`(강한 영문 디스플레이), 본문: `Noto Sans KR`, 정보성 텍스트: `DM Mono`
- 사진: 객실 사진은 전체를 설명하기보다 일부를 크게 잘라 보여주어 스크롤 동기를 만듭니다.
- 모바일: 메뉴와 객실은 한 열로 바뀌며, 손가락으로 누르기 쉬운 큰 예약 버튼을 유지합니다.
